#ifndef ENGINE_H
#define ENGINE_H


#include <d3d12.h>
#include <dxgi.h>
#include <dxgi1_4.h>
#include "ComPtr.h"
#include <DirectXTex.h>
#include <d3dx12.h>

#pragma comment(lib, "d3d12.lib") // d3d12???C?u???????????N????
#pragma comment(lib, "dxgi.lib") // dxgi???C?u???????????N????

class Engine
{
public:
	enum { FRAME_BUFFER_COUNT = 2 };
	static constexpr DXGI_FORMAT kDepthStencilFormat = DXGI_FORMAT_D32_FLOAT; // ?_?u???o?b?t?@?????O??????2

public:
	bool Init(HWND hwnd, UINT windowWidth, UINT windowHeight); // ?G???W????????

	void BeginRender(); // ?`???J?n????
	void EndRender(); // ?`???I??????

public: // ?O????A?N?Z?X?????????Getter???????J???????
	ID3D12Device6* Device();
	ID3D12GraphicsCommandList* CommandList();
	ID3D12CommandAllocator* Allocator(UINT index);
	ID3D12CommandQueue* Queue();
	UINT CurrentBackBufferIndex();
	// IBL????????V???b?g?R?}???h???L?^?????????BClose ?? Execute ?? Wait ???s??
	void ExecuteAndWait();
	// HDR/PostProcess: メイン描画先HDRリソース・RTV・バックバッファ
	ID3D12Resource* GetHdrColorResource();
	ID3D12Resource* GetBackBufferResource();
	D3D12_CPU_DESCRIPTOR_HANDLE GetBackBufferRtvCpuHandle();
	D3D12_CPU_DESCRIPTOR_HANDLE GetHdrRtvCpuHandle();
	D3D12_CPU_DESCRIPTOR_HANDLE GetDsvCpuHandle();
	UINT GetFrameBufferWidth() const { return m_FrameBufferWidth; }
	UINT GetFrameBufferHeight() const { return m_FrameBufferHeight; }

private: // DirectX12????????g?????????
	bool CreateDevice(); // ?f?o?C?X???
	bool CreateCommandQueue(); // ?R?}???h?L???[???
	bool CreateSwapChain(); // ?X???b?v?`?F?C?????
	bool CreateCommandList(); // ?R?}???h???X?g??R?}???h?A???P?[?^?[???
	bool CreateFence(); // ?t?F???X???
	void CreateViewPort(); // ?r???[?|?[?g???
	void CreateScissorRect(); // ?V?U?[??`???

private: // ?`???g??DirectX12??I?u?W?F?N?g????
	HWND m_hWnd;
	UINT m_FrameBufferWidth = 0;
	UINT m_FrameBufferHeight = 0;
	UINT m_CurrentBackBufferIndex = 0;

	ComPtr<ID3D12Device6> m_pDevice = nullptr; // ?f?o?C?X
	ComPtr<ID3D12CommandQueue> m_pQueue = nullptr; // ?R?}???h?L???[
	ComPtr<IDXGISwapChain3> m_pSwapChain = nullptr; // ?X???b?v?`?F?C??
	ComPtr<ID3D12CommandAllocator> m_pAllocator[FRAME_BUFFER_COUNT] = { nullptr }; // ?R?}???h?A???P?[???[
	ComPtr<ID3D12GraphicsCommandList> m_pCommandList = nullptr; // ?R?}???h???X?g
	HANDLE m_fenceEvent = nullptr; // ?t?F???X??g???C?x???g
	ComPtr<ID3D12Fence> m_pFence = nullptr; // ?t?F???X
	UINT64 m_fenceValue[FRAME_BUFFER_COUNT]; // ?t?F???X??l?i?_?u???o?b?t?@?????O?p??2??j
	D3D12_VIEWPORT m_Viewport; // ?r???[?|?[?g
	D3D12_RECT m_Scissor; // ?V?U?[??`

private: // ?`???g???I?u?W?F?N?g???????????????
	bool CreateRenderTarget(); // ?????_?[?^?[?Q?b?g???
	bool CreateDepthStencil(); // ?[?x?X?e???V???o?b?t?@???

	UINT m_RtvDescriptorSize = 0; // ?????_?[?^?[?Q?b?g?r???[??f?B?X?N???v?^?T?C?Y
	ComPtr<ID3D12DescriptorHeap> m_pRtvHeap = nullptr; // ?????_?[?^?[?Q?b?g??f?B?X?N???v?^?q?[?v (FRAME_BUFFER_COUNT + 1: 最後がHDR用)
	ComPtr<ID3D12Resource> m_pRenderTargets[FRAME_BUFFER_COUNT] = { nullptr }; // ?????_?[?^?[?Q?b?g?i?_?u???o?b?t?@?????O??????2??j
	ComPtr<ID3D12Resource> m_pHdrColor = nullptr; // メイン描画用 HDR R16G16B16A16_FLOAT

	UINT m_DsvDescriptorSize = 0; // ?[?x?X?e???V????f?B?X?N???v?^?[?T?C?Y
	ComPtr<ID3D12DescriptorHeap> m_pDsvHeap = nullptr; // ?[?x?X?e???V????f?B?X?N???v?^?q?[?v
	ComPtr<ID3D12Resource> m_pDepthStencilBuffer = nullptr; // ?[?x?X?e???V???o?b?t?@?i????????1???????j

private: // ?`???[?v??g?p???????
	ID3D12Resource* m_currentRenderTarget = nullptr; // ?????t???[????????_?[?^?[?Q?b?g?????I??????????????
	void WaitRender(); // ?`???????????
};

extern Engine* g_Engine; // ??????????Q??????????O???[?o???????

#endif // !ENGINE_H